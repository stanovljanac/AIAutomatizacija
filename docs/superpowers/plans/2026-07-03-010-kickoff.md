# Kickoff — Video 010 (next session start here)

**One-line:** produce video **010**, the pilot of the channel's Phase-2 "trust design in AI
workflows" direction — a Desk Notes news piece + paired Short about a lawyer suspended for filing
AI-generated fake case citations. **Nothing has been implemented yet.** This file is the cold-start
handoff.

## Read first (in order)
1. **The design spec (source of truth):**
   `docs/superpowers/specs/2026-07-03-a1-trust-design-video-design.md` — read it fully before acting.
2. `CLAUDE.md` (router) + `docs/WORKFLOW.md` (production steps + gates).
3. Memory: [[channel-altitude-shift]], [[bespoke-first-video-system]], [[no-title-card-scenes]],
   [[strategist-scene-standard]], [[on-screen-source-for-stats]], [[lead-with-copy-pasteable-prompt]],
   [[proof-must-be-visible]], [[seo-at-script-approval]], [[thumbnails-prompts-not-auto]],
   [[qa-panel-use-gemini]], [[use-f-drive-not-c]], [[commit-workflow]].

## Locked decisions (do not re-litigate)
- **Video #:** 010. Folder: `content/010-<slug>/` from `content/_TEMPLATE/` (git-ignored per repo rule).
- **Lane/archetype:** Desk Notes (news). Long ~5–6 min + paired Short ~45–55s. Bespoke HyperFrames-first.
- **Real subject:** *trust design in AI workflows* (flagship). NOT "AI hallucinations 101."
- **Hook line:** "A lawyer got suspended after filing AI-generated fake cases."
- **Thesis (creator-defining):** "The model didn't lie. It generated. The failure happened at the
  point of trust."
- **Grounding framing:** behavior principle — "fail loudly, not silently" / "AI that refuses when it
  can't anchor to a source." **Never say "RAG."** Never "RAG solves hallucinations."
- **Closing line:** "Good AI doesn't always answer. Good AI knows when it doesn't know."
- **Takeaway artifact:** copy-pasteable **cite-or-refuse prompt** (draft in spec §6), pause-&-screenshot
  on screen, full text in description.
- **Stat budget = exactly 2 on-screen numbers:** (1) the human anchor "X of Y citations were fake"
  (candidate: Nebraska, ~57/63 — verify); (2) the pattern = documented-case count from **Damien
  Charlotin's database** (damiencharlotin.com/hallucinations). Both with on-screen sources. NOT the
  $145k figure. Everything else is background VO only.
- **Tone guardrail:** analytical/forensic "serious AI systems thinking" — NOT true-crime / "AI fear."
  Brand black+gold; the only alarm imagery is the functional NOT FOUND / REVOKED stamps.

## Implementation sequence (walk the pipeline; honor the gates)
1. **Scaffold:** copy `content/_TEMPLATE/` → `content/010-<pick-slug>/`. Add to `ideas.json`/brief per
   `/novi-video` + WORKFLOW. Lane `desk-notes` (or `ai-news` if framed as roundup — it's not; keep
   `desk-notes`). value_type/takeaway filled.
2. **fact-check FIRST — make-or-break** (`.claude/skills/fact-check/SKILL.md`). This video's thesis is
   verification, so our own facts must be airtight. Verify every candidate claim against **primary**
   sources (court/bar orders, the Charlotin DB). Write `sources.md` + `claims.json`. **Cut or soften
   anything that doesn't verify** — do not smooth over. Confirm the exact 2 on-screen numbers survive.
3. **Write the brief** (angle = spec §2; the 3 corrections = spec §3 baked in).
4. **script-writing** (`.claude/skills/script-writing/SKILL.md`) → `script.json`, following the film
   beats in spec §5. Bespoke scenes, no title cards. Then **script-review** panel (claude-subagent +
   gemini-3.5-flash — the Gemini key works, see [[qa-panel-use-gemini]]). Loop to green.
5. **Gate 1 is auto** (topic+angle+type already decided here) — go straight through. **Gate 2 = script
   gate:** owner approves the script. Generate SEO (title/desc/tags + Short caption) at approval
   ([[seo-at-script-approval]]).
6. **voice-synthesis** (edge-tts draft + faster-whisper align) → **storyboard** → **video-render**
   (combo) for long + Short. 2 thumbnail PROMPTS only (never auto).
7. **qa-video** gate (sync, ≤2-line captions/safe-zone, no title-card scenes, bespoke ratio, exactly
   2 sourced on-screen numbers, one continuous audio track). Auto-fix technical; flag content.
8. **Gate 3 = final-video gate:** owner watches + picks thumbnail. Then Azure final voice → re-align →
   re-render → `youtube-publish` as PRIVATE draft. Owner sets "altered content = yes" + publishes.

## Do NOT
- Do **not** commit or push anything unless the owner explicitly asks ([[commit-workflow]]). The design
  spec + this file are the only tracked changes so far and are **left uncommitted** by owner's standing
  rule.
- Do **not** build the pipeline "RAG"/grounding engine here — it's a **separate later spec** (spec
  §10). This video only *demonstrates* the principle.
- Do **not** write temp/render output to C: — use F: ([[use-f-drive-not-c]]).
- Do **not** fabricate any fact, number, quote, or case name (the whole point).

## C1 Short (spec §8)
One visible escalating chain: fake citation → magnifier NOT FOUND → most flip red → REVOKED → "one
rule could've stopped it" → cite-or-refuse flash → close line. Same analytical tone. One-line
cross-post caption.

## State of the repo right now
- Written earlier (uncommitted): the design spec + this kickoff.
- Also modified in the working tree from before (pre-existing, unrelated to 010): `docs/PROGRESS.md`,
  `pipeline/shared/schemas/brief.schema.json`, `pipeline/shared/schemas/ideas.schema.json`,
  `style/CHANNEL.md` — check `git status` and don't clobber.
- Memory updated: `channel-altitude-shift.md` (+ MEMORY.md index line).

---

## ▶▶ PROGRESS — Session 2 (2026-07-03/04): RENDERED + QA GREEN + OWNER REVISION PASS. AWAITING GATE ③. RESUME HERE. ◀◀

### Owner revision pass (2026-07-04) — all 6 points fixed + verified + re-rendered
Owner reviewed the first render and flagged 6 issues; all fixed and re-rendered:
1. **No blank scene starts** — 6 HF scenes (three-stack, citation-build, counter-map, trust-gap,
   plausible-true, closing) were holding blank while the VO talked because their first reveal was
   tied to a later sentence beat. Reworked each timeline to establish content at sentence 0 (e.g.
   trust-gap now shows a dim 3-node skeleton immediately, then activates on cue).
2. **Number out of frame** — `stat-grid` restructured to number-BESIDE-grid (landscape) / smaller
   sizes (portrait) so the "57" is fully in frame; verified both orientations.
3. **Text overflow** — `closing` line sizes + max-width fixed so the two lines wrap inside the frame.
4. **Prompt scene** — replaced the corner-PiP + zoom `prompt-focus` with a NEW bespoke HF
   **`prompt-card`** scene: full-frame, centered, large, legible, NO zoom, NO corner, nothing clipped
   (long s11 + Short s6). (Owner rule: never corner+zoom a prompt, esp. on Shorts.)
5. **90% speed** — added a per-video `brief.json.voice_rate` override (make_voice.py + make_voice_azure.py;
   Short inherits parent brief); set **`voice_rate: "-12%"`** on 010 → ~90% of prior pace. Re-voiced +
   re-aligned (long **261s**, Short **67s**) + re-compiled ALL HF clips at new timings + re-rendered both.
6. (bespoke count is now **14 HF scenes** — added `prompt-card`.)
**Both QA gates GREEN again (10/10). 513 tests green.** ⚠ Long Remotion render OOMs if run concurrently
with other renders/compiles — render it ALONE with `--concurrency=4` (RAM, not disk; F: has space).



**Status: STOPPED AT GATE ③ (final-video gate) — owner watches + picks a thumbnail.** Script was
approved ("approve and implement"). Nothing committed (owner's standing rule).

### Done this session (steps 6–7)
- **Voice + align** (edge-tts draft + faster-whisper): long `alignment.json` = **231s / 61 sentences**;
  Short = **58s / 25 sentences**. (edge-tts emitted no WordBoundary → used `make_alignment.py`.)
- **Bespoke-first build (owner chose FULL bespoke): 13 new HyperFrames scenes** authored + render-tested
  under `templates/hyperframes/scenes/`: `cite-collapse` (hook), `three-stack`, `citation-build`,
  `stat-grid` (#1: 57/63 + source), `counter-map` (#2: 1,695 + source), `trust-gap`, `plausible-true`,
  `good-vs-bad`, `magnifier-notfound` (Short), `suspended-stamp` (Short), `desk-pivot` (s04/s09
  transitions), `human-gate` (s12), `closing` (s13/Short-s7 CTA). Every scene is black+gold (hero/close)
  or blue+gold (body); the only alarm imagery is the functional NOT FOUND / SUSPENDED stamps.
- **scene-plan.json** (long, 13 scenes — ALL bespoke: 12 HF + 1 custom `prompt-focus`) +
  hand-authored **short/scene-plan.json** (7 scenes — 6 HF + 1 custom). Both validate.
- **Rendered (combo)**: `video/final.mp4` (235s, 23 MB) + `short/video/final.mp4` (60.5s, 9 MB).
- **QA gate GREEN**: long + Short each **10/10 checks pass** (`qa.report.json`). `no_title_card` was the
  only initial failure — fixed by converting the 4 remaining Remotion cards (2 section-headers, 1
  term-highlight, 1 cta-card) into the new `desk-pivot`/`human-gate`/`closing` bespoke HF scenes. **All
  513 unit tests green.** Both on-screen numbers verified + sourced on-screen (57/63 Kingston; 1,695
  Charlotin as of Jul 3 2026 — matches fact-check, no re-pull drift today).
- **2 thumbnail PROMPTS** in `visual-prompts.json` (never auto). **SEO** finalized (rec title: "AI Wrote
  Fake Court Cases. A Lawyer Got Suspended."). brief.status = `qa_passed`.

### NEXT STEP (Gate ③)
- **Owner watches `video/final.mp4` + `short/video/final.mp4`, generates 1 of the 2 thumbnail prompts,
  drops the chosen image in `images/`.** On approval: Azure final voice (`--final`) → re-align →
  re-render (HF clips are cached, only Remotion re-runs) → composite the thumbnail via `ThumbComposite`
  → `youtube-publish` as PRIVATE draft. Owner sets "Altered content = Yes" + publishes.
- **Do NOT commit/push** unless the owner explicitly asks ([[commit-workflow]]).

---

## ▶▶ (Session 1 archive) STOPPED AT GATE ② (script gate). ◀◀

**Status: STOPPED AT GATE ② (script gate) — awaiting owner approval of the script.** Nothing
committed (owner's standing rule). Everything below is on disk under `content/010-point-of-trust/`
(git-ignored per repo rule). Slug chosen: **`point-of-trust`**.

### Done (steps 1–5 of the sequence above)
1. **Scaffolded** `content/010-point-of-trust/` from `_TEMPLATE` via
   `node pipeline/00-ideas/new-video.mjs point-of-trust "AI wrote the citations. A lawyer signed them."`
   — includes the lean `short/` sub-unit.
2. **fact-check DONE (make-or-break gate PASSED)** — verified against **primary** sources; wrote
   `sources.md` + `claims.json` (draft mode, pass: true, 0 unverifiable retained claims). The
   **exactly-2 on-screen numbers survived**, both locked with on-screen source tags:
   - **#1 human anchor = "57 of 63 citations defective"** — *Kingston v. Kingston*, No. S-25-050,
     Nebraska Supreme Court (opinion Mar 20 2026); attorney **Greg Lake** **suspended** mid-April
     2026. Court's own tally: 20 hallucinations incl. **4 wholly fabricated cases**. Source tag:
     `Nebraska Supreme Court — Kingston v. Kingston (2026)`.
   - **#2 pattern = "1,695 cases worldwide, and counting"** — Damien Charlotin's *AI Hallucination
     Cases Database* (damiencharlotin.com/hallucinations), **last updated 3 Jul 2026**. Pulled LIVE
     (WebFetch was 403-blocked → fetched with a browser UA; the page's own two internal aggregates
     both reconcile to 1,695). Source tag: `damiencharlotin.com/hallucinations — as of 3 Jul 2026`.
     ⚠️ **`stale-risk` (claims.json c2): RE-PULL the live count at QA/publish** and update the
     number + "as of" date; script says "and climbing" so small drift stays correct.
   - Guardrails honored: **never "RAG"**; **NOT** "first in US" (primary sources only support
     "first for Nebraska"); `$145k` figure deliberately not used.
3. **Brief written** (`brief.json`, `status: "scripted"`) — angle = spec §2 with the 3 corrections
   baked in; `lane: desk-notes`, `value_type`/`takeaway`/`value_score 9.2`/`value_band: qualify`.
4. **Scripts written + validated** (both PASS `pipeline/shared/validate.js`):
   - **Long** `script.json` — 13 scenes, ~5:30, follows spec §5 film beats; bespoke HyperFrames-first
     with rich per-scene `notes` (no title cards); thesis + closing line landed; artifact = the
     cite-or-refuse prompt (pause & screenshot).
   - **Short** `short/script.json` — 7 scenes, ~52s, one visible escalating chain (spec §8). **Fixed
     spec §8's "REVOKED" → "SUSPENDED"** for accuracy (temporary suspension, not disbarment).
5. **script-review panel PASSED** (`script.review.json`) — claude-subagent (Sonnet) **9.0** +
   **gemini-3.5-flash 9.6** (key verified working, [[qa-panel-use-gemini]]); **all 4 hard gates pass**
   on both. Author merged Sonnet's cheap minors (answer-first margin, de-choppied s03, s11 semicolon
   → period). Gemini's notes already handled (stale-risk count; scenes 7–9 dynamism in the notes).
6. **SEO drafted at the gate** (`seo.json`, [[seo-at-script-approval]]) — title options (rec: "AI
   Wrote Fake Court Cases. A Lawyer Got Suspended."), keyword-first description w/ full cite-or-refuse
   prompt, tags, chapter labels (timestamps = placeholders, fill from `alignment.json`), pinned
   comment, Short cross-post caption. `altered_content: true`.

### Files now in `content/010-point-of-trust/`
`brief.json` (scripted) · `sources.md` · `claims.json` (pass) · `script.json` (valid) ·
`script.review.json` (pass) · `seo.json` (draft) · `short/script.json` (valid).

### NEXT STEP (start of next session)
- **This is GATE ② — a HARD human gate. Get the owner's script decision FIRST** (approve as-is /
  approve with tweaks / request changes). Do NOT voice or render before approval.
- **On approval:** set `brief.status: "script_approved"`, finalize `seo.json` (owner's title pick),
  then walk step 6 of the sequence: **voice-synthesis** (edge-tts draft + faster-whisper align, long
  + Short) → **storyboard** (`scene-plan.json`, realize the bespoke `notes` into HyperFrames scenes,
  no title cards) → **video-render** (combo) → **2 thumbnail PROMPTS only** (never auto,
  [[thumbnails-prompts-not-auto]]).
- Then **qa-video** gate (sync, ≤2-line captions/safe-zone, no title-card scenes, bespoke ratio,
  exactly 2 sourced on-screen numbers, one continuous audio track; re-pull the 1,695 count) →
  **GATE ③** owner watches + picks thumbnail → Azure final voice → re-align → re-render →
  `youtube-publish` as PRIVATE draft (owner sets "altered content = yes" + publishes).
- **Do NOT commit/push** unless the owner explicitly asks ([[commit-workflow]]).
